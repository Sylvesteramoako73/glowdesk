rules_version = '2';
// All reads and writes go through the Firebase Admin SDK (server actions + API routes).
// The Admin SDK bypasses these rules, so denying all client access here is correct and safe.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
